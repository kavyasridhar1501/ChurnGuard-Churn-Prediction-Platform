# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=churnguard
POSTGRES_USER=churnguard_user
POSTGRES_PASSWORD=churnguard_password

# Database URL (async)
DATABASE_URL=postgresql+asyncpg://churnguard_user:churnguard_password@localhost:5432/churnguard

# MLflow Configuration
MLFLOW_TRACKING_URI=http://localhost:5001
MLFLOW_BACKEND_STORE_URI=postgresql://churnguard_user:churnguard_password@localhost:5432/churnguard
MLFLOW_ARTIFACT_ROOT=./mlartifacts

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=True
API_LOG_LEVEL=info

# CORS Settings
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Azure Storage (for data download)
AZURE_STORAGE_CONNECTION_STRING=

# Model Configuration
MODEL_PATH=./models
MODEL_REGISTRY_PATH=./models/registry

# Great Expectations
GE_DATA_CONTEXT_ROOT=./great_expectations

# Environment
ENVIRONMENT=development
DEBUG=True
